# ExternalSecret example (preferred when External Secrets Operator is installed).
#
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: semantic-pokedex-streamlit
#   namespace: semantic-pokedex
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     kind: ClusterSecretStore
#     name: aws-secrets-manager
#   target:
#     name: semantic-pokedex-streamlit-secret
#   data:
#     - secretKey: SEARCH_API_URL
#       remoteRef:
#         key: semantic-pokedex/streamlit
#         property: SEARCH_API_URL

# Plain Secret fallback (for local experiments only):
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: semantic-pokedex-streamlit-secret
#   namespace: semantic-pokedex
# type: Opaque
# stringData:
#   SEARCH_API_URL: "https://replace-with-api-gateway-url/search"
