apiVersion: v1
kind: Service
metadata:
  name: semantic-pokedex-streamlit
  namespace: semantic-pokedex
spec:
  type: ClusterIP
  selector:
    app: semantic-pokedex-streamlit
  ports:
    - name: http
      port: 80
      targetPort: 8501
